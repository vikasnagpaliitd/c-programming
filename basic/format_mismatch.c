// What happens in case of format mismatch?
#include <stdio.h>

int main()
{
  float pi = 3.14;

  printf("%d", pi); // What will be the output?

  // Similarly, what happens if format mismatch in scanf call?

  return 0;
} 
